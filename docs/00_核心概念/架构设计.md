# 拟态架构设计

## Electron 主进程 / 渲染进程分工

```
┌──────────────────────────────────────────────────────┐
│                  Electron 主进程 (main/)               │
│                                                      │
│  ┌──────────┐  ┌──────────┐  ┌───────────────────┐  │
│  │ config.js│  │window.js │  │music-detector.js  │  │
│  │ 配置加载  │  │透明窗口   │  │ 进程列表轮询       │  │
│  └──────────┘  └──────────┘  └───────────────────┘  │
│  ┌──────────┐  ┌──────────┐  ┌───────────────────┐  │
│  │ tray.js  │  │ipc-      │  │position-store.js  │  │
│  │ 系统托盘  │  │handlers  │  │ 窗口位置持久化     │  │
│  └──────────┘  └──────────┘  └───────────────────┘  │
│                                                      │
│   启动流程: config → window → tray → IPC → shortcuts  │
├──────────────────────────────────────────────────────┤
│              IPC 通信（扁平参数传递）                  │
│  window-drag | resize-window | set-pet-size | ...    │
├──────────────────────────────────────────────────────┤
│                Electron 渲染进程 (renderer/)           │
│                                                      │
│  ┌───────────┐  ┌───────────┐  ┌────────────────┐   │
│  │   FSM     │  │ Rendering │  │    Bubble      │   │
│  │  状态机   │  │  绘制引擎  │  │    气泡系统     │   │
│  └───────────┘  └───────────┘  └────────────────┘   │
│  ┌───────────┐  ┌───────────┐  ┌────────────────┐   │
│  │  Layout   │  │   Audio   │  │  Interaction   │   │
│  │  布局引擎  │  │  音效合成  │  │   交互模块      │   │
│  └───────────┘  └───────────┘  └────────────────┘   │
│                                                      │
│  脚本加载顺序: mimic → fsm → rendering → audio       │
│              → layout → bubble → interaction → app   │
├──────────────────────────────────────────────────────┤
│       HTTP API → 晓风后端 (127.0.0.1:5001)           │
│       POST /api/file/process | /api/chat             │
└──────────────────────────────────────────────────────┘
```

## 渲染进程模块依赖

```
mimic.js (全局命名空间 window.Mimic)
  ├── fsm/
  │   ├── expressions.js       ← 表情定义表
  │   ├── fsm-core.js          ← FSM 引擎
  │   └── states/*.js          ← 各状态处理器
  ├── rendering/
  │   ├── pixel-character.js   ← 16×20 网格像素小人
  │   └── particles.js         ← 6 种粒子特效
  ├── layout.js                ← 动态窗口布局
  ├── audio.js                 ← Web Audio 音效
  ├── bubble/bubble.js         ← 消息队列 + 定位
  └── interaction/
      ├── drag.js              ← 拖拽移动
      ├── contextmenu.js       ← 右键菜单
      ├── drop.js              ← 文件拖放
      ├── eye-tracking.js      ← 眼球追踪
      ├── click.js             ← 点击反应
      ├── chat.js              ← 对话交互
      └── music-detect.js      ← 音乐检测前端
```

## FSM 状态机设计

### 核心 API

```javascript
M.FSM.transitionTo('stateName', { context })  // 切换状态
M.FSM.update(performance.now())               // 每帧更新
M.FSM.getExpression()                         // 获取当前表情
```

### 生命周期

```
_exit(oldState) → _enter(newState) → _update(newState, now) [每帧]
```

### 状态转换图

```
                    ┌──────────┐
         文件拖放     │  working  │
      ┌─────────────→│ (进食动画) │──── 完成 ──┐
      │              └──────────┘            │
      │                                       ↓
  ┌───┴────┐    点击头部    ┌───────┐    自动恢复    ┌──────┐
  │  idle   │──────────────→│ happy  │─────────────→│ idle  │
  │ (默认)  │               │(1.5s)  │              │       │
  └───┬────┘    点击身体    ┌──────┐               └──────┘
      │──────────────────→│surprised│──── 自动恢复 ──→ idle
      │                    │(1.5s)  │
      │                    └────────┘
      │  输入"bongo"       ┌────────┐
      │──────────────────→│ bongo   │──── 10s ──→ idle
      │                    └────────┘
      │  输入"听歌"/自动检测  ┌───────────┐
      │──────────────────→│ listening  │
      │                    │(手动退出)   │
      │                    └───────────┘
      │  60s无操作          ┌────────┐
      ├──────────────────→│ sleepy  │←── 鼠标移动唤醒
      │  120s无操作         └────────┘
      ├──────────────────→┌──────────┐
      │                    │ sleeping │←── 鼠标移动唤醒 + 伸懒腰
      │                    └──────────┘
      │  (预留)            ┌────────┐
      └──────────────────→│ alert   │
                           └────────┘
```

## 关键设计决策

| 决策 | 理由 | 日期 |
|------|------|------|
| Electron 而非 C# WinForms | 跨平台潜力；Canvas/CSS 更适合像素渲染；与晓风 Node.js 技术栈统一 | 2026-06 |
| Canvas 2D 而非 WebGL | 16×20 像素网格不需要 GPU 渲染；像素控制更精确 | 2026-06 |
| FSM 而非行为树 | 状态数有限（~8个）；逻辑直观清晰；代码量小 | 2026-06 |
| Web Audio 合成而非音频文件 | 无外部依赖；合成音效符合像素风格；体积小 | 2026-06 |
| 全局命名空间而非 ES Module | Electron 渲染进程直接加载 script，无打包工具 | 2026-06 |
| IPC 扁平参数 | 绕过上下文隔离的对象序列化边缘情况 | 2026-06 |
| 项目从晓风主仓库分离 | 独立版本管理；降低晓风仓库复杂度 | 2026-06 |
| 布局公式 `winH = petSize + 2×max(overhead, BOTTOM_MARGIN)` | 角色绝对居中 + 气泡完整可见 | 2026-06 |

## 数据流

```
用户拖放文件 → drop.js 递归复制 → working 状态 + 进度气泡
                ↓ 完成
           POST /api/file/process → 晓风后端处理

用户输入文字 → chat.js
                ├── 彩蛋关键词 → 触发 bongo/listening
                ├── apiEnabled=true → POST /api/chat → 晓风回复
                └── apiEnabled=false → Mock 关键词匹配

音乐检测 (3s轮询) → tasklist 扫描进程
                ├── 匹配到播放器 → music-playing IPC → listening 状态
                └── 播放器消失 → music-stopped IPC → 回到 idle
```
